library(MASS)
options(digits=5)

initSpeed = 4;
mult = 1.5;
cap = initSpeed * mult;


func = function(x) {initSpeed * x^2 - x};

curve( func,
       from = 1,
       to = 1.5,
       main = "Curve",
       ylab = "speed",
       xlab = "time",
       type = "l",
       col = "black"
)

